<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 15 (API 35+) theme override.

  Place this file at:
    android/app/src/main/res/values-v35/styles.xml

  On Android 15, edge-to-edge is mandatory and cannot be opted out of
  (windowOptOutEdgeToEdgeEnforcement was removed in API 35). Capacitor 8
  already embraces edge-to-edge, so this file simply reinforces transparent
  bars and white icons — identical to values/styles.xml.

  The app background (#070B17) is visible behind both bars because the
  WebView fills the full screen in edge-to-edge mode. CSS env(safe-area-inset-*)
  keeps content within the visible area above/below the bars.
-->
<resources>
  <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="colorPrimary">@color/appBackground</item>
    <item name="colorPrimaryDark">@color/appBackground</item>
    <item name="colorAccent">@color/appBackground</item>
    <item name="android:windowBackground">@color/appBackground</item>

    <!-- Transparent bars — mandatory in Android 15 edge-to-edge -->
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>

    <!-- Light icons (white) on dark background -->
    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowLightNavigationBar">false</item>
  </style>
</resources>
