<?xml version="1.0" encoding="utf-8"?>
<!--
  Android theme for English Verb Trainer.

  Place this file at:
    android/app/src/main/res/values/styles.xml

  after running `npx cap add android`, replacing the existing content.

  Capacitor 8 enables edge-to-edge by default (BridgeActivity calls
  WindowCompat.setDecorFitsSystemWindows(window, false)). We embrace this
  and make both bars transparent so the app background (#070B17) shows
  through them. Layout insets are handled in CSS via env(safe-area-inset-*).
-->
<resources>
  <!-- Base application theme (referenced by AndroidManifest.xml) -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="colorPrimary">@color/appBackground</item>
    <item name="colorPrimaryDark">@color/appBackground</item>
    <item name="colorAccent">@color/appBackground</item>
    <item name="android:windowBackground">@color/appBackground</item>

    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@color/appBackground</item>

    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowLightNavigationBar">false</item>
  </style>

  <!-- Alias used by BridgeActivity internally -->
  <style name="AppTheme.NoActionBar" parent="AppTheme" />

  <!-- Splash screen theme — shown before the webview loads -->
  <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/appBackground</item>
    <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
  </style>
</resources>
